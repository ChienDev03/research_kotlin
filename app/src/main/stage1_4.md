# Layouts và Modifiers

- Layout cơ bản: `Column`, `Row`, `Box`
- `LazyColumn`, `LazyRow` cho nội dung scroll
- Nested layouts
- Modifier cơ bản: padding, spacing, alignment, click listener
- `ConstraintLayout` trong Compose
- Intrinsic measurements

# Material Design & UI Elements

- Styling Text
- Buttons và clickable components
- Images và icons
- Cards, surfaces, layout containers
- Input fields: `TextField`, `CheckBox`, `RadioButton`, `Switch`

# Theming và Styling

- `MaterialTheme`
- Tùy chỉnh colors, typography, shapes
- Light/Dark theme
- Dynamic color (Material 3)
- Xây dựng design system tái sử dụng

# Xử lý Input & Form Validation

- `TextField`, `OutlinedTextField`, `CheckBox`, `RadioButton`, `Switch`
- Quản lý input state
- Keyboard handling và focus control
- Validate thủ công, hiển thị lỗi (VD: mật khẩu không khớp, số điện thoại)

# Animation & Gesture

- `animate*AsState`, `AnimatedVisibility`, `Crossfade`, `AnimatedContent`
- Custom animation spec
- Gesture: click, long press, double tap, swipe, scroll, `Modifier.draggable`, `pointerInput`, `detectTapGestures`, multi-touch (zoom, rotate)
- Animation nâng cao: shared element transition, AnimatedVectorDrawable, MotionLayout với Compose, physics-based animation
